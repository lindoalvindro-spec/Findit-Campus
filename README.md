# FindIt Campus

Full-stack application for finding and reporting lost items on campus.

## Structure
- `frontend/` - React Application (Vite)
- `backend/` - Node.js Express API (Optional/Future use)
- `database/` - Supabase SQL schemas
